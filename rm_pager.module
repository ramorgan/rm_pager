<?php

/**
 * Implements hook_views_api().
 */
function rm_pager_views_api() {
  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'rm_pager') . '/includes/views',
    'template path' => drupal_get_path('module', 'rm_pager') . '/themes',
  );
}